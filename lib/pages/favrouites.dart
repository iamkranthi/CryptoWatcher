import 'package:flutter/material.dart';

class Favrouites extends StatelessWidget {
  const Favrouites({super.key});

  @override
  Widget build(BuildContext context) {
    return const Expanded(
      child: Text("data"),
    );
  }
}